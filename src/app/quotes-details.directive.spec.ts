import { QuotesDetailsDirective } from './quotes-details.directive';

describe('QuotesDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new QuotesDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
